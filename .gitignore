cosign.key
_build_*
output
_build-*/**
*_chunkah_*
*_rpm-ostree_*

# ClipFrost (proprietary) is checked out here at build time, never committed
/clipfrost-src/*
!/clipfrost-src/.keep
/framemeter/*.so
